using System;
using System.Collections.Generic;

namespace algoritmos_busqueda_dfs {
    class IDPFS {

        /* static void Main(string[] args) {
            DLS reinas = new DLS();

            bool hasResult = false;
            int limit = 2;

            while(!hasResult) {
                reinas.currStateLvl = new KeyValuePair<int, int[]>(1, new int[]{1, 1, 1, 1});
                reinas.frontier.Push(reinas.currStateLvl);
                hasResult = reinas.depthLimitedSearch(reinas.frontier, limit, 0);
                limit += 2;
            }
        } */
    }
}
